# genel ortam
ENV=dev # dev | prod

# veri dizinleri
DATA_DIR=./data
PARQUET_DIR=./data/parquet
RAW_DIR=./data/raw

# duckdb veritabanı
DUCKDB_PATH=./warehouse/transfermarkt.duckdb

# kaggle veri seti adı
KAGGLE_DATASET=davidcariboo/player-scores

# --- Serving API (GitHub Releases) ---
# Paths are chosen by ENV. Override as needed.
DEV_DB_PATH=warehouse/transfermarkt_serving.duckdb
PROD_DB_PATH=/tmp/transfermarkt_serving.duckdb
RELEASE_DB_URL=https://github.com/<user>/<repo>/releases/download/<TAG>/transfermarkt_serving.duckdb
RELEASE_DB_SHA256=https://github.com/<user>/<repo>/releases/download/<TAG>/transfermarkt_serving.duckdb.sha256
